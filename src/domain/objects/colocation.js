export class Colocation {
  constructor ({ id, name, price, user, images, adress }) {
    this.id = id
    this.name = name
    this.price = price
    this.user = user
    this.images = images
    this.adress = adress
  }
}
